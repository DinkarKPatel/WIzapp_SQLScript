CREATE PROCEDURE SP3S_DELETE_UPLOAD_WSRMERGE_TABLES
@nSpId VARCHAR(50)
AS
BEGIN
	
	DELETE A FROM WSR_CNM01106_UPLOAD A WITH (ROWLOCK)  WHERE sp_id=@nSpId
	
	DELETE A FROM WSR_CNd01106_UPLOAD A WITH (ROWLOCK)   WHERE sp_id=@nSpId

	DELETE A from WSR_XN_AUDIT_TRIAL_DET_UPLOAD A  WITH (ROWLOCK)   WHERE sp_id=@nSpId

	DELETE A  FROM WSR_PMT01106_UPLOAD a WITH (ROWLOCK)   WHERE sp_id=@nSpId
	
END