CREATE FUNCTION FN_GETMINFINYEARLOCOB 
(@CDEPTID CHAR(2),
@CACCODE CHAR(10))
RETURNS VARCHAR(10)
--WITH ENCRYPTION
AS
BEGIN
	DECLARE @CFINYEAR VARCHAR(10)
	SELECT @CFINYEAR=MIN(FIN_YEAR) FROM LOCOB 
	WHERE AC_CODE = @CACCODE 
	AND DEPT_ID = @CDEPTID

	RETURN @CFINYEAR
END
