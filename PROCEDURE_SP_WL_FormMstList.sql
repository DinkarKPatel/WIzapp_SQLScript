

-- GET ALL DEFINED FORM MASTER
CREATE PROCEDURE SP_WL_FORMMSTLIST
--WITH ENCRYPTION
AS

	SELECT FORM_ID , FORM_NAME, TAX_PERCENTAGE 
	FROM FORM(NOLOCK) 
	WHERE FORM_ID <>'0000000' AND INACTIVE=0
	ORDER BY FORM_NAME
