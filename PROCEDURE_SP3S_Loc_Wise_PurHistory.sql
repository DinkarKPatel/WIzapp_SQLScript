CREATE PROCEDURE SP3S_LOC_WISE_PURHISTORY
(
@ARTICLE_CODE CHAR(9)
)
AS
BEGIN
    SELECT (A.DEPT_ID+'  '+ DEPT_NAME)DEPT_NAME,WSP,RSP FROM LOC_WISE_PURHISTORY A(NOLOCK)
    JOIN LOCATION B (NOLOCK) ON A.DEPT_ID=B.DEPT_ID
    WHERE ARTICLE_CODE=@ARTICLE_CODE

END
