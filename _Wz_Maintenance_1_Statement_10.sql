

IF NOT EXISTS (SELECT CONFIG_OPTION FROM CONFIG WHERE CONFIG_OPTION='UPDATE_MRR_CREATION_DEPT_ID_2' AND VALUE=1)
BEGIN

    UPDATE PIM01106 SET MRR_CREATION_DEPT_ID=(CASE WHEN ISNULL(ACCOUNTS_DEPT_ID,'')<>'' THEN ACCOUNTS_DEPT_ID
	    ELSE LOCATION_CODE END) WHERE ISNULL(MRR_CREATION_DEPT_ID,'')=''
	    
	IF NOT EXISTS (SELECT CONFIG_OPTION FROM CONFIG WHERE CONFIG_OPTION='UPDATE_MRR_CREATION_DEPT_ID_2')
		INSERT CONFIG (CONFIG_OPTION,VALUE,ROW_ID,LAST_UPDATE)
			   VALUES ('UPDATE_MRR_CREATION_DEPT_ID_2',1,'',GETDATE())	
	ELSE
		UPDATE CONFIG SET VALUE=1 WHERE CONFIG_OPTION='UPDATE_MRR_CREATION_DEPT_ID_2'
  
END		
