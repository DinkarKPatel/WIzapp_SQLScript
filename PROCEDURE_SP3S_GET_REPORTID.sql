CREATE PROCEDURE SP3S_GET_REPORTID(@REP VARCHAR(100),@MOD VARCHAR(40))
AS
BEGIN
DECLARE @REPID VARCHAR(40)
SELECT TOP 1 ROW_ID REPORT_ID
INTO #TEMP
FROM GST_REPORT_CONFIG WHERE XN_TYPE=@MOD AND [FILE_NAME]=@REP
SELECT * FROM #TEMP
END
