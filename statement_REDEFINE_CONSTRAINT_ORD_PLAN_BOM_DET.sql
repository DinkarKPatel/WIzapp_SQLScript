DECLARE @cCmd NVARCHAR(MAX)
IF EXISTS (SELECT * FROM INFORMATION_SCHEMA.TABLE_CONSTRAINTS WHERE CONSTRAINT_NAME='UNQ_REF_ROW_ID_MEMO_ARTICLE')
BEGIN
	SET @cCmd=N'alter table ORD_PLAN_BOM_DET drop constraint UNQ_REF_ROW_ID_MEMO_ARTICLE'
	EXEC SP_EXECUTESQL @cCmd
END

DECLARE @cCmd1 NVARCHAR(MAX)
SET @cCmd1=N'alter table ORD_PLAN_BOM_DET add constraint UNQ_REF_ROW_ID_MEMO_ARTICLE unique (ARTICLE_CODE,memo_id,ref_row_id,JOB_CODE)'
EXEC SP_EXECUTESQL @cCmd1
