CREATE  PROCEDURE SP3S_INSERTDEFAULTBIN
AS
BEGIN

INSERT BIN_LOC	( DEPT_ID, LAST_UPDATE, BIN_ID )   
SELECT A.DEPT_ID, GETDATE(), '000'  FROM  
LOCATION A 
LEFT OUTER JOIN BIN_LOC B ON A.DEPT_ID= B.DEPT_ID AND '000' = B.BIN_ID
WHERE B.BIN_ID IS NULL

END
