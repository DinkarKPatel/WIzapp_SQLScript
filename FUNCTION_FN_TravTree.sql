CREATE FUNCTION FN_TRAVTREE ( @CUSERCODE VARCHAR(20) )
RETURNS VARCHAR(2000)
--WITH ENCRYPTION 
AS 
BEGIN
	DECLARE @CRETVAL VARCHAR(2000)
	SET @CRETVAL = ''
	
	SET @CRETVAL = '''' + @CUSERCODE + ''''
	SELECT @CRETVAL = @CRETVAL + DBO.FN_TRAVTREESTR( @CUSERCODE )

	RETURN @CRETVAL
END
