
;WITH CTE AS 
(
 SELECT PARA2_NAME,PARA2_SET ,SR=ROW_NUMBER () OVER (PARTITION BY PARA2_NAME,PARA2_SET ORDER BY PARA2_NAME,PARA2_SET)
 FROM DBO.PARA2
)

UPDATE  CTE SET PARA2_SET =PARA2_SET +CAST(SR AS VARCHAR(10)) WHERE SR>1

