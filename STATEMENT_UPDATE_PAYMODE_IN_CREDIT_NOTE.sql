IF NOT EXISTS (SELECT CONFIG_OPTION FROM CONFIG WHERE  CONFIG_OPTION='UPDATE_PAYMODE_IN_CN' AND VALUE=1)
BEGIN
	PRINT ' UPDATE PAYMODE IN CN '
	
	UPDATE CNM01106 SET pay_mode=4 WHERE ISNULL(PAY_MODE,0)<=1

	UPDATE CNM01106 SET pay_mode=1 WHERE ISNULL(PAY_MODE,0)=2

	IF NOT EXISTS (SELECT CONFIG_OPTION FROM CONFIG WHERE CONFIG_OPTION='UPDATE_PAYMODE_IN_CN')
		INSERT CONFIG (CONFIG_OPTION,VALUE,ROW_ID,LAST_UPDATE)
			   VALUES ('UPDATE_PAYMODE_IN_CN',1,'',GETDATE())	
	ELSE
		UPDATE CONFIG SET VALUE='1' WHERE CONFIG_OPTION='UPDATE_PAYMODE_IN_CN'
	
END		
