CREATE VIEW VW_CON_CONSUMPTION_LIST 
--WITH ENCRYPTION
AS 
	SELECT MEMO_NO 
	,CONVERT(CHAR(10),MEMO_DT,105) AS MEMO_DATE 
	,CASE WHEN CANCELLED =0 THEN '' ELSE 'CANCELLED' END AS CANCELLED
	,MEMO_ID,FIN_YEAR
	FROM CON_CONSUMPTION_MST
