CREATE PROCEDURE SP_LOCWISE_OPS
--WITH ENCRYPTION

AS 
BEGIN
	SELECT A.DEPT_ID,A.DEPT_NAME,B.OPENING_CASH_DATE ,B.OPENING_CASH_BALANCE 
	FROM LOCATION A (NOLOCK)
	LEFT OUTER JOIN LOC_REQ B (NOLOCK) ON A.DEPT_ID= B.DEPT_ID
	WHERE A.INACTIVE=0 AND LOC_TYPE= 1 AND A.DEPT_ID= A.MAJOR_DEPT_ID 
END
