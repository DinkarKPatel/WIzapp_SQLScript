CREATE VIEW VW_XNRECONM_PRINT

AS
SELECT T1.*,USERS.USERNAME,EDTUSERS.USERNAME AS EDT_USERNAME  FROM XNRECONM T1 
JOIN USERS ON T1.USER_CODE =  USERS.USER_CODE 
JOIN USERS EDTUSERS ON T1.EDT_USER_CODE =  EDTUSERS.USER_CODE 
WHERE 1=2
