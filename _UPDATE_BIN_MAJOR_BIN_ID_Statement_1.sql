IF NOT EXISTS (SELECT CONFIG_OPTION FROM CONFIG WHERE CONFIG_OPTION='UPDATE_BIN_MAJOR_BIN_ID' AND VALUE=1)
BEGIN
	IF NOT EXISTS (SELECT CONFIG_OPTION FROM CONFIG WHERE CONFIG_OPTION='UPDATE_BIN_MAJOR_BIN_ID')
		INSERT CONFIG (CONFIG_OPTION,VALUE,ROW_ID,LAST_UPDATE)
			   VALUES ('UPDATE_BIN_MAJOR_BIN_ID',1,'',GETDATE())	
	ELSE
		UPDATE CONFIG SET VALUE=1 WHERE CONFIG_OPTION='UPDATE_BIN_MAJOR_BIN_ID'
	
	   UPDATE BIN SET MAJOR_BIN_ID =BIN_ID WHERE ISNULL(MAJOR_BIN_ID,'')=''
END		




