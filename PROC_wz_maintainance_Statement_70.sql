

IF NOT EXISTS (SELECT CONFIG_OPTION FROM CONFIG WHERE CONFIG_OPTION='APM01106_DEPT_ID' AND VALUE='1')
BEGIN
	UPDATE APM01106 SET DEPT_ID=LEFT(MEMO_ID,2) WHERE ISNULL(DEPT_ID,'')=''
	
	IF NOT EXISTS(SELECT CONFIG_OPTION FROM CONFIG WHERE CONFIG_OPTION='APM01106_DEPT_ID')
       INSERT CONFIG (CONFIG_OPTION,VALUE,ROW_ID,LAST_UPDATE)
	   VALUES ('APM01106_DEPT_ID','1','',GETDATE())	
	ELSE	
		UPDATE CONFIG SET VALUE='1' WHERE CONFIG_OPTION='APM01106_DEPT_ID'	
		
END
