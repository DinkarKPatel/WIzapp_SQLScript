CREATE PROCEDURE SP_RETAILSALE_27--(LocId 3 digit change only increased the parameter width by Sanjay:01-11-2024)
(  
	 @CQUERYID			NUMERIC(2),  
	 @CWHERE			VARCHAR(MAX)='',  
	 @CFINYEAR			VARCHAR(5)='',  
	 @CDEPTID			VARCHAR(4)='',  
	 @NNAVMODE			NUMERIC(2)=1,  
	 @CWIZAPPUSERCODE	VARCHAR(10)='',  
	 @CREFMEMOID		VARCHAR(40)='',  
	 @CREFMEMODT		DATETIME='',  
	 @BINCLUDEESTIMATE	BIT=1,  
	 @CFROMDT			DATETIME='',  
	 @CTODT				VARCHAR(50)='',
	 @bCardDiscount		BIT=0,
	 @cCustCode			VARCHAR(15)=''
) 
AS  
BEGIN  
	IF @cWhere<>''
		SELECT REF_CM_ID AS CM_ID,CM_ID AS PACK_SLIP_ID,LAST_UPDATE FROM RPS_MST (NOLOCK) WHERE REF_CM_ID=@CWHERE
	ELSE
		SELECT REF_CM_ID AS CM_ID,CM_ID AS PACK_SLIP_ID,LAST_UPDATE FROM RPS_MST (NOLOCK) WHERE 1=2
end
