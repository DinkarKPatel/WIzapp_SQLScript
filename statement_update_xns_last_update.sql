DECLARE @DCUTOFFDATE DATETIME,@CHODEPTID VARCHAR(2),@LOCID VARCHAR(20)

SELECT @CHODEPTID=value FROM CONFIG WHERE CONFIG_OPTION='LOCATION_ID'
SELECT @LOCID=value FROM CONFIG WHERE CONFIG_OPTION='ho_LOCATION_ID'

SET @DCUTOFFDATE='2020-04-01'
if isnull(@CHODEPTID,'')<>isnull(@LOCID,'')
begin

	UPDATE  POM01106 SET LAST_UPDATE=getdate() WHERE PO_DT>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  PIM01106 SET LAST_UPDATE=getdate() WHERE receipt_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  SNC_MST SET LAST_UPDATE=getdate() WHERE receipt_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  SCM01106 SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  IRM01106 SET LAST_UPDATE=getdate() WHERE IRM_memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  APM01106 SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  APPROVAL_RETURN_MST SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  TILL_SHIFT_MST SET LAST_UPDATE=getdate() WHERE open_date>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  RPS_MST SET LAST_UPDATE=getdate() WHERE cm_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  WPS_MST SET LAST_UPDATE=getdate() WHERE ps_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  INM01106 SET LAST_UPDATE=getdate() WHERE inv_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  CMM01106 SET LAST_UPDATE=getdate() WHERE cm_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  ARC01106 SET LAST_UPDATE=getdate() WHERE adv_rec_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  TILL_LIFTS SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  TILL_EXPENSE_MST SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  TILL_BANK_TRANSFER SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  PEM01106 SET LAST_UPDATE=getdate() WHERE pem_memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  WSL_ORDER_MST SET LAST_UPDATE=getdate() WHERE order_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  BUYER_ORDER_MST SET LAST_UPDATE=getdate() WHERE order_dt >=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  DNPS_MST SET LAST_UPDATE=getdate() WHERE ps_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  RMM01106 SET LAST_UPDATE=getdate() WHERE rm_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  CNM01106 SET LAST_UPDATE=getdate() WHERE receipt_dt >=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  FLOOR_ST_MST SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  JOBWORK_ISSUE_MST SET LAST_UPDATE=getdate() WHERE issue_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  JOBWORK_RECEIPT_MST SET LAST_UPDATE=getdate() WHERE receipt_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  ICM01106 SET LAST_UPDATE=getdate() WHERE cnc_memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  PO_ADJ_MST SET LAST_UPDATE=getdate() WHERE memo_DT>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  DAYCLOSE_LOG SET LAST_UPDATE=getdate() WHERE Log_Date>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  HOLD_BACK_DELIVER_MST SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  POST_SALES_JOBWORK_ISSUE_MST SET LAST_UPDATE=getdate() WHERE issue_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  POST_SALES_JOBWORK_RECEIPT_MST SET LAST_UPDATE=getdate() WHERE receipt_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  SLS_DELIVERY_MST SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  STK_RECON_HIST_MST SET LAST_UPDATE=getdate() WHERE RECON_START_DT>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  PCI_MST SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  PCO_MST SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  DSM01106 SET LAST_UPDATE=getdate() WHERE ds_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  PARCEL_MST SET LAST_UPDATE=getdate() WHERE parcel_memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  ORD_PLAN_MST SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  BOM_ISSUE_MST SET LAST_UPDATE=getdate() WHERE issue_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  TRANSFER_TO_TRADING_MST SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  SLR_RECON_MST SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  GRN_PS_MST SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''
	UPDATE  MANUAL_STOCK_COUNT_XN_mst SET LAST_UPDATE=getdate() WHERE memo_dt>=@DCUTOFFDATE AND isnull(LAST_UPDATE,'')=''

END