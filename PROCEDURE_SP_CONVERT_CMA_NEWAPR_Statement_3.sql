

IF NOT EXISTS (SELECT CONFIG_OPTION FROM CONFIG WHERE CONFIG_OPTION='CONVERT_CMA_NEWAPR' AND VALUE=1)
BEGIN
	PRINT 'UPDATING INVOICE MODE COLUMN IN PIM01106'

	IF NOT EXISTS (SELECT CONFIG_OPTION FROM CONFIG WHERE CONFIG_OPTION='CONVERT_CMA_NEWAPR')
		INSERT CONFIG (CONFIG_OPTION,VALUE,ROW_ID,LAST_UPDATE)
			   VALUES ('CONVERT_CMA_NEWAPR','1','',GETDATE())	
	ELSE
		UPDATE CONFIG SET VALUE='1' WHERE CONFIG_OPTION='CONVERT_CMA_NEWAPR'

	EXEC SP_CONVERT_CMA_NEWAPR		
END		

