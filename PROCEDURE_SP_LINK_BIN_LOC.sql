CREATE PROCEDURE SP_LINK_BIN_LOC
(	
	@CDEPT_ID	VARCHAR(5)
)
--WITH ENCRYPTION
AS
BEGIN
	IF NOT EXISTS(SELECT BIN_ID FROM BIN_LOC WHERE BIN_ID='000' AND DEPT_ID=@CDEPT_ID)
	BEGIN
		INSERT INTO BIN_LOC(BIN_ID,DEPT_ID,LAST_UPDATE)
		VALUES( '000' ,@CDEPT_ID ,GETDATE())
	END
END
