CREATE FUNCTION DBO.FN_INTRIM(@STR VARCHAR(MAX))
RETURNS VARCHAR(MAX)
AS
BEGIN
--REMOVE MULTIPLE SPACE INTO SINGLE SPACE IN A STRING
WHILE CHARINDEX('  ',@STR)>0
  SET @STR=LEFT(@STR,CHARINDEX('  ',@STR)-1)+LTRIM(SUBSTRING(@STR,CHARINDEX('  ',@STR)+1,4000))
RETURN @STR
END
