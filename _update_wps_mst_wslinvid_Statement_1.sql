PRINT 'UPDATING WHOLESALE INVOICE ID IN PACK SLIP MASTER TABLE'
UPDATE a SET WSL_INV_ID=b.ps_id FROM wps_mst a 
JOIN ind01106 b (NOLOCK) ON a.ps_id=b.ps_id 
JOIN INM01106 c ON c.INV_ID=B.INV_ID
WHERE c.CANCELLED=0 AND c.entry_mode=2 AND ISNULL(wsl_inv_id,'')=''
