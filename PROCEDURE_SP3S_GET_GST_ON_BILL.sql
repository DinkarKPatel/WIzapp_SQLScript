CREATE PROCEDURE SP3S_GET_GST_ON_BILL(@FROM DATE,@TILL DATE)
AS
BEGIN
SET NOCOUNT ON
SELECT DISTINCT D.CM_ID,D.GST_PERCENTAGE
FROM CMM01106 M (NOLOCK) JOIN CMD01106 D(NOLOCK) ON M.CM_ID=D.CM_ID
WHERE M.CM_DT BETWEEN @FROM AND @TILL AND M.CANCELLED=0
SET NOCOUNT OFF
END
