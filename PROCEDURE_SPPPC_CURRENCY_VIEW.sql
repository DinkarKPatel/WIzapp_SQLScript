CREATE PROCEDURE SPPPC_CURRENCY_VIEW
AS
BEGIN
   SELECT A.CURRENCY_CODE,A.CURRENCY_NAME 
   FROM PPC_CURRENCY_MST A (NOLOCK)
   ORDER BY CASE CURRENCY_NAME WHEN 'INR' THEN 1 ELSE 2 END,CURRENCY_NAME 
END
