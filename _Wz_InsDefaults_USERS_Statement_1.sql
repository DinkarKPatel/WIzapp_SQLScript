--*** DEFAULT USER ENTRY IN USER TABLE
IF NOT EXISTS ( SELECT USER_CODE FROM USERS WHERE USER_CODE = '0000000' )
BEGIN
	
	IF EXISTS (SELECT NAME FROM SYSOBJECTS WHERE NAME='TRG_USERS_CHKPWD')
		ALTER TABLE USERS DISABLE TRIGGER TRG_USERS_CHKPWD

	INSERT USERS ( USER_CODE, USERNAME, PASSWD, MAJOR_USER_CODE, USER_ALIAS,  
				   LAST_UPDATE, DISCOUNT_PERCENTAGE_LEVEL, 
				    VIEW_DATA_AFTER_DFM,  INACTIVE,LOC_PASSWD,ROLE_ID )
			VALUES ('0000000', 'SUPER', '', '0000000', '00',  
					'', 0, 0,  0,'','0000000')

	IF EXISTS (SELECT NAME FROM SYSOBJECTS WHERE NAME='TRG_USERS_CHKPWD')
		ALTER TABLE USERS ENABLE TRIGGER TRG_USERS_CHKPWD

END
