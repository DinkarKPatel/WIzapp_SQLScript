CREATE PROCEDURE SP3s_GET_FIXED_PRODUCTCODES
(
	@cArticleCode	VARCHAR(50)
)
AS
BEGIN
	SELECT SKU.PRODUCT_CODE
	FROM SKU 
	WHERE BARCODE_CODING_SCHEME=1 AND SKU.PRODUCT_CODE NOT LIKE '%@%'
	AND SKU.ARTICLE_CODE=@cArticleCode
END

